// export function parseNumberString(str) {
//   const cleaned = str.replace(/,/g, ""); // Remove all commas
//   const number = parseFloat(cleaned); // Convert to number (preserves decimals)
//   return isNaN(number) ? null : number;
// }
